{
  "name": "aicomplyr",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "build": "npm run build --workspaces",
    "lint": "npm run lint --workspaces",
    "test": "npm run test --workspaces",
    "dev:api": "npm run dev --workspace=@aicomplyr/api",
    "start:api": "npm run start --workspace=@aicomplyr/api"
  }
}
