export function track(_name: string, _properties?: Record<string, unknown>) {
  // No-op placeholder for analytics integration.
  // Keeping API shape compatible with the Lovable AuthHub components.
}

