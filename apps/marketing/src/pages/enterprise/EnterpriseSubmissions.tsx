import React from 'react';
import { EnhancedSubmissionReview } from '@/components/enterprise/EnhancedSubmissionReview';

const EnterpriseSubmissions = () => {
  return <EnhancedSubmissionReview />;
};

export default EnterpriseSubmissions;